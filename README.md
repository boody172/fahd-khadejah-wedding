# Fahd & Khadejah — Wedding Invitation

An animated, single-page wedding invitation. Vite + React + TypeScript + Tailwind CSS + Framer Motion, with Supabase for RSVPs.

## Local development

```bash
npm install
cp .env.example .env.local   # fill in Supabase URL + publishable key
npm run dev
```
